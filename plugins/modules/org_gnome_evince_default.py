#!/usr/bin/python
# -*- coding: utf-8 -*-

# Copyright (c) 2023, Jules
# All rights reserved.

from __future__ import absolute_import, division, print_function
__metaclass__ = type

DOCUMENTATION = r'''
---
module: org_gnome_evince_default
short_description: Manages GNOME GSettings for org.gnome.Evince.Default
description:
  - >
    This module allows for the configuration of GSettings keys within the 'org.gnome.Evince.Default' schema.
  - >
    It supports setting default values and locking keys to enforce system-wide policies.
author:
  - Autogenerated by ansible-gnome-lockdown
version_added: "1.1.0"
options:
  enable-spellchecking:
    description:
    - Check spelling when writing annotation notes or filling text forms.
    - Description - Schema Blank
    type: bool
    default: true

  enable-spellchecking_locked:
    description:
    - >
      If set to true, locks the 'enable-spellchecking' key to prevent user modification.
    type: bool
    default: false
  show-sidebar:
    description:
    - Show a side bar next to the document to help navigate the document.
    - Description - Schema Blank
    type: bool
    default: true

  show-sidebar_locked:
    description:
    - >
      If set to true, locks the 'show-sidebar' key to prevent user modification.
    type: bool
    default: false
  window-ratio:
    description:
    - Summary - Schema Blank
    - Description - Schema Blank
    type: str
    default: (0.0, 0.0)

  window-ratio_locked:
    description:
    - >
      If set to true, locks the 'window-ratio' key to prevent user modification.
    type: bool
    default: false
  sizing-mode:
    description:
    - Summary - Schema Blank
    - Description - Schema Blank
    type: str
    default: automatic

  sizing-mode_locked:
    description:
    - >
      If set to true, locks the 'sizing-mode' key to prevent user modification.
    type: bool
    default: false
  zoom:
    description:
    - Set the default zoom level to display a document.
    - Description - Schema Blank
    type: float
    default: 1.0

  zoom_locked:
    description:
    - >
      If set to true, locks the 'zoom' key to prevent user modification.
    type: bool
    default: false
  inverted-colors:
    description:
    - Summary - Schema Blank
    - Description - Schema Blank
    type: bool
    default: false

  inverted-colors_locked:
    description:
    - >
      If set to true, locks the 'inverted-colors' key to prevent user modification.
    type: bool
    default: false
  continuous:
    description:
    - Display the document as a continuous stream of pages instead of page at a time.
    - Description - Schema Blank
    type: bool
    default: true

  continuous_locked:
    description:
    - >
      If set to true, locks the 'continuous' key to prevent user modification.
    type: bool
    default: false
  dual-page:
    description:
    - Show two pages side by side.
    - Description - Schema Blank
    type: bool
    default: false

  dual-page_locked:
    description:
    - >
      If set to true, locks the 'dual-page' key to prevent user modification.
    type: bool
    default: false
  dual-page-odd-left:
    description:
    - Show the first page (odd page) on the left (when displaying two pages side by side)
    - Description - Schema Blank
    type: bool
    default: false

  dual-page-odd-left_locked:
    description:
    - >
      If set to true, locks the 'dual-page-odd-left' key to prevent user modification.
    type: bool
    default: false
  fullscreen:
    description:
    - Summary - Schema Blank
    - Description - Schema Blank
    type: bool
    default: false

  fullscreen_locked:
    description:
    - >
      If set to true, locks the 'fullscreen' key to prevent user modification.
    type: bool
    default: false
  sidebar-page:
    description:
    - Summary - Schema Blank
    - Description - Schema Blank
    type: str
    default: links

  sidebar-page_locked:
    description:
    - >
      If set to true, locks the 'sidebar-page' key to prevent user modification.
    type: bool
    default: false
  sidebar-size:
    description:
    - Summary - Schema Blank
    - Description - Schema Blank
    type: int
    default: 132

  sidebar-size_locked:
    description:
    - >
      If set to true, locks the 'sidebar-size' key to prevent user modification.
    type: bool
    default: false
'''

EXAMPLES = r'''
- name: Configure and lock GNOME desktop settings for org.gnome.Evince.Default
  org_gnome_evince_default:
    enable-spellchecking: true

    show-sidebar: true

    window-ratio: (0.0, 0.0)

    sizing-mode: automatic

    zoom: 1.0

    inverted-colors: false

    continuous: true

    dual-page: false

    dual-page-odd-left: false

    fullscreen: false

    sidebar-page: links

    sidebar-size: 132

'''

RETURN = r'''
# Default return values
'''

from ansible.module_utils.basic import AnsibleModule
import os
import configparser

def main():
    """Main function for the Ansible module."""
    # We have to do gross stuff with the bools here.
    keys_spec = {
        'enable-spellchecking': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'show-sidebar': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'window-ratio': {
            'default': '(0.0, 0.0)',
            'type': 'str',
            'gtype':'(dd)',
        },
        'sizing-mode': {
            'default': 'automatic',
            'type': 'str',
            'gtype':'s',
        },
        'zoom': {
            'default': 1.0,
            'type': 'float',
            'gtype':'d',
        },
        'inverted-colors': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'continuous': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'dual-page': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'dual-page-odd-left': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'fullscreen': {
            'default': False,
            'type': 'bool',
            'gtype':'b',
        },
        'sidebar-page': {
            'default': 'links',
            'type': 'str',
            'gtype':'s',
        },
        'sidebar-size': {
            'default': 132,
            'type': 'int',
            'gtype':'i',
        },
    }

    argument_spec = {}
    for key_name, spec in keys_spec.items():
        argument_spec[key_name] = spec
        argument_spec[f"{key_name}_locked"] = {'type': 'bool', 'default': False}

    module = AnsibleModule(
        argument_spec=argument_spec,
        supports_check_mode=True
    )

    changed = False

    settings_dir = "/etc/dconf/db/local.d"
    locks_dir = os.path.join(settings_dir, "locks")

    settings_file_path = os.path.join(settings_dir, "00-ansible-org.gnome.Evince.Default")
    lock_file_path = os.path.join(locks_dir, "ansible-org.gnome.Evince.Default")

    schema_full_path = "/org/gnome/evince/default/"
    config_section = schema_full_path.strip('/')

    os.makedirs(settings_dir, exist_ok=True)
    os.makedirs(locks_dir, exist_ok=True)

    # Idempotency: Read current settings
    current_settings = configparser.ConfigParser()
    if os.path.exists(settings_file_path):
        current_settings.read(settings_file_path)

    # Idempotency: Read current locks from our dedicated lock file
    current_locks = []
    if os.path.exists(lock_file_path):
        with open(lock_file_path, 'r') as f:
            current_locks = [line.strip() for line in f if line.strip()]

    # Prepare settings and locks based on module parameters
    new_settings = configparser.ConfigParser()
    new_settings.add_section(config_section)
    new_locks = []

    for key_name, spec in keys_spec.items():
        param_value = module.params[key_name]
        param_locked = module.params[f"{key_name}_locked"]

        if spec['type'] == 'bool':
            setting_value = str(param_value).lower()
        elif spec['type'] == 'str':
            setting_value = f'{param_value}'
        else:
            setting_value = str(param_value)

        new_settings.set(config_section, key_name, setting_value)

        if param_locked:
            new_locks.append(f"{schema_full_path}{key_name}")

    # Check for changes
    settings_changed = False
    if not current_settings.has_section(config_section) or \
       (current_settings.has_section(config_section) and dict(current_settings.items(config_section)) != dict(new_settings.items(config_section))):
        settings_changed = True

    locks_changed = sorted(new_locks) != sorted(current_locks)

    if settings_changed or locks_changed:
        changed = True

    if changed and not module.check_mode:
        # Write settings file
        with open(settings_file_path, 'w') as f:
            new_settings.write(f, space_around_delimiters=False)

        # Manage the dedicated lock file
        if new_locks:
            with open(lock_file_path, 'w') as f:
                for lock_key in new_locks:
                    f.write(f"{lock_key}\n")
        elif os.path.exists(lock_file_path):
            os.remove(lock_file_path)

        # Update dconf database
        module.run_command(["dconf", "update"], check_rc=True)

    module.exit_json(changed=changed)

if __name__ == '__main__':
    main()
